"""| Keyword    | Purpose / Use                                        |
| ---------- | ---------------------------------------------------- |
| `False`    | Boolean false value                                  |
| `None`     | Represents no value or null                          |
| `True`     | Boolean true value                                   |
| `and`      | Logical AND operator                                 |
| `as`       | Give an alias or rename while importing or in `with` |
| `assert`   | Debugging; checks if a condition is true             |
| `async`    | Declares asynchronous functions                      |
| `await`    | Waits for an asynchronous task                       |
| `break`    | Exits a loop immediately                             |
| `class`    | Defines a class                                      |
| `continue` | Skips the current loop iteration                     |
| `def`      | Defines a function                                   |
| `del`      | Deletes variables or objects                         |
| `elif`     | Else-if condition                                    |
| `else`     | Executes when previous conditions are false          |
| `except`   | Handles exceptions                                   |
| `finally`  | Always executes after `try`                          |
| `for`      | Creates a for loop                                   |
| `from`     | Imports specific items from a module                 |
| `global`   | Declares a global variable                           |
| `if`       | Conditional statement                                |
| `import`   | Imports modules                                      |
| `in`       | Membership operator / used in loops                  |
| `is`       | Identity operator                                    |
| `lambda`   | Creates an anonymous function                        |
| `nonlocal` | Refers to a variable in the enclosing function       |
| `not`      | Logical NOT operator                                 |
| `or`       | Logical OR operator                                  |
| `pass`     | Empty placeholder statement                          |
| `raise`    | Raises an exception                                  |
| `return`   | Returns a value from a function                      |
| `try`      | Starts exception handling                            |
| `while`    | Creates a while loop                                 |
| `with`     | Resource management (files, locks, etc.)             |
| `yield`    | Returns values one by one from a generator           |"""
